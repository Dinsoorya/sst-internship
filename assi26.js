
const readline=require("readline")
.createInterface({
    input:process.stdin,
   output:process.stdin,
});
readline.question("enter the limit of array:",(arr)=>{
    readline.question("enter the numbers:",(num)=>{
        let array=[];
        //for(i=0;i<arr;i++)
    
    
        //
            array.push(num);
            
        //
        console.log(array);
        readline.close();
});
     });
    