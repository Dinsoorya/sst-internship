const readline=require("readline")
.createInterface({
    input:process.stdin,
    output:process.stdout,
});
readline.question("enter the name:",(nam)=>{
    const data=function(x)
    {
        let a;
        a=x;
        return a;
    }
        var z
        z=data(nam)
        console.log("hello " +z);
        readline.close();


    
    

});

