const readline=require("readline")
.createInterface
({
    input:process.stdin,
    output:process.stdout,
});

readline.question("enter the numbers:",(arr)[10]=>{
    var arr=parseInt(a[10])
    
});

